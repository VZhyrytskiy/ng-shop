import { ClickPropertyChangerDirective } from './click-property-changer.directive';

describe('ClickPropertyChangerDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickPropertyChangerDirective();
    expect(directive).toBeTruthy();
  });
});
